# 名片宝（客户背景调研工具）· 项目登记资料

> 按《项目登记与归档资料采集规范》第 9 节模板整理。所有 API Key 均已隐藏，不出现任何明文凭据。

### 基础信息

- **项目名称**：名片宝（客户背景调研工具）
- **简称 / 系统名**：名片宝 / Customer Research
- **项目描述**：面向外贸展会拓客场景的客户背景调研工具。销售在展会收到名片后拍照上传，系统 OCR 识别名片信息（客户名可人工校正），一键调用「公开搜索 + AI 大模型」生成结构化客户背景报告（含合作匹配度评估、图片墙、视频卡），并沉淀进客户库批量管理、筛选与导出。
- **项目目标 / 业务价值**：把"展会收名片 → 人工查资料 → 写客户档案"从数十分钟缩短到一次上传 + 数十秒，提升拓客线索转化与跟进效率，沉淀可检索的客户资产。
- **需求说明**：用户在展会现场用手机拍照名片上传，系统 OCR 提取姓名/公司/电话/邮箱/网站/国家/品类等字段并允许人工校正（尤其客户名）；随后基于公开搜索（Serper / DuckDuckGo 等）与 DeepSeek 大模型生成 10 维度结构化调研报告（含合作匹配度评估、图片墙、视频卡）；报告可保存，客户库支持状态流转（新建→已调研→已联系→商机）、搜索/筛选与 CSV/Excel/JSON 批量导出。约束：内网 http 部署，需兼容移动端现场拍照。
- **所属部门 / 类型**：外贸数字化（EGO 国际）/ 内部效率工具·客户调研
- **负责人 / 创建人 / 维护人**：待确认（EGO 外贸团队），维护人同负责人
- **优先级 / 状态**：中 / 已归档（软件运行中）
- **开始 / 完成 / 验收日期**：待确认（无明确记录，不补造）
- **验收人 / 验收说明**：由项目负责人确认；功能验收见下方维护说明与运行验证（/api/health 正常、调研全链路跑通、示例客户入库）

### 部署记录

1. **环境**：生产（内网）
   **主机名称**：内网服务器（业务管理部）
   **IP / 域名**：192.168.1.246
   **端口 / 协议**：7004 / http
   **服务名 / 容器名**：customer-research（Docker Compose 服务名）；容器 customer-research-7004
   **部署路径 / 运行时 / 数据库**：仓库目录 `customer-research`；Python 3.12-slim + uvicorn（后端 8000）+ 静态前端；SQLite（`./data/customer_research.db`）
   **当前版本**：latest（镜像 customer-research:latest，基于 Dockerfile 构建）
   **运行状态 / 最后验证**：运行中 / 2026-09-20
   **备注**：单容器同时托管 FastAPI 后端与静态前端；搜索源 Serper、AI 模型 DeepSeek、视觉 OCR 支持本地 Tesseract 兜底与视觉模型；密钥存于服务端，不外露。

### 文档

- **README.md**｜类型：部署与运维说明｜版本：v1.0｜来源：仓库根目录｜说明：启动/停止/更新/回滚/日志位置、API Key 配置（含设置页运行时改 Key 即时生效）

### 图片

1. **名片宝 - 录入工作台（首页）**｜展示功能：名片拍照上传、OCR 识别、基本信息表单、开始调研按钮、顶部三步流程步骤条「拍照上传→OCR 识别→AI 调研出报告」｜采集入口：浏览器访问 http://192.168.1.246:7004/ 进入「名片宝」视图｜文件路径：`screenshots/screenshot-entry.png`｜已脱敏：是（页面不含密钥）
2. **名片宝 - 客户库列表与调研结果**｜展示功能：客户列表（含匹配度徽章、状态流转）、示例客户 Dollar Tree 调研记录（匹配度「高」）、搜索与 CSV/Excel/JSON 批量导出｜采集入口：同上站点点击侧栏「客户库」｜文件路径：`screenshots/screenshot-library.png`｜已脱敏：是
3. **名片宝 - 设置页（API Key 配置）**｜展示功能：运行时配置 DeepSeek Key、视觉模型 Provider 与 Key、搜索源 Provider 与 Serper/Tavily/Brave Key；密钥仅服务端存储、页面以掩码显示、保存即时生效无需重启｜采集入口：同上站点点击侧栏「设置」｜文件路径：`screenshots/screenshot-settings.png`｜已脱敏：是（所有 Key 均为掩码 `••••••••`）

### 归档与维护

- **当前软件版本 / 状态**：latest / 运行中
- **特殊注意事项**：
  1. API Key（DeepSeek / Serper / Tavily / 视觉模型）属敏感凭据，存于服务端 `.env` 与 `config.json`，禁止写入代码或公开文档，页面仅以掩码显示；
  2. 视觉 OCR 未配置视觉 Key 时自动回退本地 Tesseract，识别偏弱需人工校正客户名；
  3. 搜索引擎默认 DuckDuckGo 免 Key（仅文本），配置 Serper 后获得图片 + 视频媒体墙；
  4. 容器内数据库位于 `./data/customer_research.db`，需随卷持久化（已挂载）。
- **维护说明**：
  - 启停：`docker-compose up -d` / `docker-compose down`
  - 更新：`docker-compose up -d --build`（代码改动需重建；仅改 API Key 用设置页，无需重建）
  - 回滚：见 Git 历史
  - 日志：`docker logs customer-research-7004`
  - 健康检查：`GET /api/health`
  - 配置接口：`GET/PUT /api/config` 支持运行时改 Key 即时生效（无需重启容器）
- **交接信息**：代码仓库 `customer-research`；部署目录同上；负责人与资料位置见上方基础信息。
- **归档确认**：待项目负责人确认资料可交接、可追溯（本资料由自动化 Agent 于 2026-09-20 按规范整理，事实均可核验）。

### 待确认项

| 字段 | 原因 | 建议确认人 |
|---|---|---|
| 项目负责人 / 部门正式归属 | 当前以 EGO 外贸团队代称，未与既有部门编码对齐 | 项目管理中心 |
| 项目编号 PRJ-2026-XXXX | 尚未分配 | 项目管理中心 |
| 视觉 OCR 免费 Key（通义千问 VL / Kimi）是否接入 | 影响名片识别精度（当前为本地 Tesseract 兜底） | 负责人 |

### 证据来源

- 代码仓库：`c:\Users\Administrator\Documents\Github\customer-research`
- 部署地址：http://192.168.1.246:7004/
- 文档：仓库 `README.md`
- 运行验证：2026-09-20 实测 `/api/health` 返回 `deepseek:true, search_provider:serper`；实测完整调研返回 10 维度报告 + 8 图 + 5 视频，示例客户成功入库。
